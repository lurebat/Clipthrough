<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Vellum</RootNamespace>
    <Description>Headless rich text document model: nodes, positions, transactions, and history. No UI dependency.</Description>
  </PropertyGroup>

  <!--
    Vellum.Core deliberately has ZERO package references, not even Avalonia.Base
    (decided 2026-08-11, docs/architecture.md 12.1). This guard turns an accidental
    dependency into a build failure rather than a silent regression.
  -->
  <Target Name="AssertNoDependencies" BeforeTargets="Build">
    <Error Condition="'@(PackageReference)' != ''"
           Text="Vellum.Core must have no PackageReference. Found: @(PackageReference). See docs/architecture.md 4.1." />
    <Error Condition="'@(ProjectReference)' != ''"
           Text="Vellum.Core must have no ProjectReference. Found: @(ProjectReference)." />
  </Target>

</Project>